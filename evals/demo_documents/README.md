# Demo Documents

These files are minimal local demo sources for manual upload/index/chat smoke checks.

- `credit_policy.md`
- `eligibility_rule.txt`

You can upload them from the UI or via `curl` to `POST /documents`.
